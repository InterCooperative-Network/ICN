[package]
name = "runtime"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
tendermint = "0.23"

[lib]
name = "runtime"
path = "src/lib.rs"
