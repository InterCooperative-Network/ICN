















}  return flagged;  });    }      flagged.push(member.id);    ) {      member.reputationGrowth > 100      member.selfApprovals > 5 ||      member.abnormalVotingPatterns ||    if (    // Heuristics: abnormal voting patterns, repeated self-approvals, sudden spikes  members.forEach(member => {  const flagged: string[] = [];export function detectFraudulentActivity(members: any[]): string[] {